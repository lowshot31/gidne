# 📋 회고 — 2026-04-03

## 📊 이번 세션 숫자

| 항목 | 값 |
|------|-----|
| 커밋 | 1개 (3378126) |
| 변경 파일 | 4개 (+184 / -49) |
| 주요 영역 | 실시간 데이터, UI 리모델링, 버그 수정 |

---

## 🚀 배포한 것

### 1. RS 리더보드 프로페셔널 리모델링
- SPY 기준 `UNDERPERFORM vs SPY` 붉은 구분선 추가
- ETF 현재 가격 & 절대 수익률 표시 (예: `$209.50 (+1.23%)`)
- 행 클릭 시 `/chart/XLK` 등 상세 차트 페이지로 네비게이션
- 순위 변동 없을 때 `──` 밑줄 제거 (깔끔하게)

### 2. Ticker Strip 바이낸스 실시간 연동
- 비트코인/이더리움 가격이 60초(야후 폴링) 대신 **~1초(바이낸스 WS)**로 즉시 반영
- `TickerStripClient.tsx`에 `useBinanceStream` 훅 직결

### 3. 가격 변동 불빛 애니메이션 수정
- CSS 클래스 토글 → **Web Animation API** 직접 DOM 제어로 전환
- 리액트 배치 업데이트가 아무리 빨라도 불빛이 100% 발동되도록 보장

### 4. 모바일/태블릿 리사이즈 깨짐 버그 수정
- `react-grid-layout`의 `minW` 값이 모바일 그리드 칸 수(4칸)를 초과하여 레이아웃 엔진이 고장나는 문제
- `minW: 6` → `minW: 2`로 조정하여 모든 뷰포트에서 정상 작동

---

## 📚 배운 것

1. **리액트 State의 한계**: 초 단위 웹소켓 환경에서 CSS 애니메이션을 State로 다루면 프레임이 무시된다 → `Element.animate()` Web API로 우회
2. **2026년 경쟁 우위의 본질**: 데이터(숫자) 자체는 무료로 널려 있음. 진짜 가치는 **"해석(RS 비교)"**과 **"내러티브(News 연동)"**에서 나온다

---

## ⚠️ 주의할 것 (기술 부채)

1. **무료 API 의존성**: 야후파이낸스 + 핀허브에 강하게 묶여 있음. 트래픽 폭증 시 Fallback 필요
2. **캐시 정책 분산**: TTL이 3초~24시간으로 파편화됨. 향후 통합 정책 검토 필요
3. **커밋 메시지 인코딩**: Windows 터미널에서 한국어 커밋 메시지가 깨짐 → **영어 커밋 메시지 사용** 결정

---

## 🎯 다음 계획

| 우선순위 | 작업 | 예상 브랜치 |
|----------|------|-------------|
| 🔴 P0 | QQQ 벤치마크 토글 추가 | `feat/qqq-benchmark` |
| 🔴 P0 | AI News Catalyst 위젯 연동 | `feat/news-catalyst` |
| 🟡 P1 | Indices/Macro/Flows 탭 구현 | `feat/tabs-v2` |

---

## 🏥 코드 건강 지표

- **같은 파일 3회 이상 수정**: `TickerStrip.tsx` (애니메이션 방식 3차 변경) → 아키텍처 스멜 아님, 기술적 최적화 과정
- **테스트 커버리지**: API 엔드포인트 기본 테스트 존재, 프론트엔드 테스트 미흡 → 다음 스프린트에서 보강 필요
